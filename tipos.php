<?php

$numerito = "23";
$new_number = $numerito + 2;

/* `var_dump()` is a PHP function that displays information about the variable passed to it, including
its data type and value. In this code, `var_dump()` and `var_dump()` are used to
display the data type and value of the variables `` and ``, respectively. */

var_dump($numerito);
var_dump($new_number);


echo "\n";